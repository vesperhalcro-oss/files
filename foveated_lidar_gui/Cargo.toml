[package]
name = "tactical_mapper"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.28"
egui = "0.28"
rusqlite = { version = "0.32", features = ["bundled"] }

[profile.release]
lto = true
strip = true
codegen-units = 1
