[package]
name = "tactical_mapper"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.28"
egui = "0.28"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync"] }
tokio-postgres = "0.7"

[profile.release]
lto = true
strip = true
codegen-units = 1
